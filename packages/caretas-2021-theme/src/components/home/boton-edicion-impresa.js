import React from 'react';
import { connect } from 'frontity';

const BotonEdicionImpresa = ({ state }) => {
    return (
        <>Boton Edicion Impresa</>
    );
};

export default connect(BotonEdicionImpresa);